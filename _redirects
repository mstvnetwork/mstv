# 1. FORCE serve the sitemap file
/sitemap.xml    /sitemap.xml    200!

# 2. Allow Google to see your SEO pages (CRITICAL FIX)
/channels_pages/*  /channels_pages/:splat  200

# 3. Clean URLs: If user goes to /9x-jalwa, show them index.html with the player
# This matches the ?ch= slug logic we added to your index.html
/channel/:slug  /index.html?ch=:slug  200

# 4. Media and Data files
/channels.json  /channels.json  200
/*.m3u8         /:splat         200
/*.mp4          /:splat         200

# 5. Catch-all (Moved to the bottom so it doesn't block the others)
/*              /index.html     200


