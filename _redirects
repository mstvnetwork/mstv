# 1. FORCE serve the sitemap file
/sitemap.xml    /sitemap.xml    200!

# 2. Allow Google to see your SEO pages (CRITICAL FIX)
/channels_pages/*  /channels_pages/:splat  200

# 3. Clean URLs
/channel/:slug  /index.html?ch=:slug  200

# 4. Media and Data files
/channels.json  /channels.json  200
/*.m3u8         /:splat         200
/*.mp4          /:splat         200

# 5. Redirect ALL remaining traffic to new site (permanent)
/*  https://ipllink.netlify.app/:splat  301!
