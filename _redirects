# 1. FORCE serve the sitemap file (Crucial Fix)
/sitemap.xml    /sitemap.xml    200!

# 2. Allow direct access to data
/channels.json  /channels.json  200

# 3. Media files
/*.m3u8         /:splat         200
/*.mp4          /:splat         200

# 4. Catch-all for SEO URLs
/*              /index.html     200
